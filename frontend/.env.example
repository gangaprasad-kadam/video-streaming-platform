# Copy this to .env and fill in values
# During development Vite proxies all /auth /videos etc. to localhost:80
# Only needed if you run frontend standalone without the Vite dev proxy

VITE_APP_NAME="Video Streaming Platform"
